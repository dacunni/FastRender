
# Make common header
ls *.h | grep -v FastRender.h | perl -pe 's/^(.*)$/#include "\1"/g' > FastRender.h

# Copy test output to S3 to see what it looks like
aws s3 cp --dryrun --recursive testoutput s3://dacunni-renders/testoutput

